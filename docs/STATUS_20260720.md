# RA_LOOP 状态与进度快照 — 20260720

> 快照时间：**2026-07-20 00:13:43 CST（UTC+08:00）**  
> 说明：下载进度是该时刻的瞬时状态，后续会继续变化。

> **状态更新：2026-07-20 09:13:55 CST** — spatial checkpoint 已完成
> 25/25 下载并通过 CPU-only 静态校验；无 `.incomplete`，四个模型分片、LoRA、
> action head、proprio projector 和 normalization key 均正确。详细 SHA-256 见
> `docs/CHECKPOINT_SPATIAL_20260720.md`。尚未启动 GPU 评测。

> **状态更新：2026-07-20 09:17:40 CST** — suite-specific spatial 启动器
> `eval/launch_oft_spatial_robot350_tmux.sh` 已准备并通过 CPU-only dry-run。
> CUDA 明确不可见；350/350 官方 Robot-init 任务严格分成 7×50，无重复、无遗漏；
> 七条命令均绑定 `/home/imc/models/ra-loop/openvla-oft-spatial`、OSMesa、seed 7
> 和物理 GPU 1--7。dry-run 后确认无输出目录、无新 tmux 会话、无 evaluator
> 进程。真实 GPU 评测尚未启动。

> **状态更新：2026-07-20 09:19:25 CST** — 经用户明确确认，spatial
> Robot-init 350 条正式评测已启动。tmux 会话为
> `ra_oft_spatial_robot350`，七个窗口分别为 `shard0_gpu1` 到
> `shard6_gpu7`，每片严格 50 条，延迟 0--180 秒启动，无 evaluator 超时。
> 启动前 GPU 1--7 均为 0% 利用率、18 MiB 基础占用且无计算进程；GPU 0 未使用。
> 首片已完成四个 checkpoint shard 的加载且无立即错误，其余片按延迟计划启动。
> 输出根目录为
> `logs/openvla_oft_spatial_robot_full350_7gpu_20260720`。当前正在运行，尚无
> 最终成功率。

> **状态更新：2026-07-20 12:35:06 CST（评测实际于 10:27:39 完成）** —
> suite-specific spatial Robot-init 正式评测验收通过：350/350、missing=0、
> duplicate=0，成功 `135/350 = 38.5714%`。七片元数据一致，无 evaluator
> 残留，GPU 1--7 已释放。相同任务上的 OFT_m 为 83/350（23.7143%），本次
> 提升 14.8571 个百分点、净增 52 个成功任务。详细记录和 SHA-256 见
> `docs/BASELINE_OFT_SPATIAL_ROBOT350_20260720.md`。

> **状态更新：2026-07-20 12:41:51 CST** — 开始下载 suite-specific
> `libero_object` checkpoint。Hugging Face 仓库为
> `moojink/openvla-7b-oft-finetuned-libero-object`，目标目录为
> `/home/imc/models/ra-loop/openvla-oft-object`，共享 tmux 会话为
> `ra_download_object`。启动前目标目录与 tmux 名称均不存在，磁盘剩余 181 GB；
> 下载进程已正常进入 25 文件获取阶段。当前只进行网络和磁盘写入，不使用 GPU，
> 尚未启动 object 评测。

> **状态更新：2026-07-20 13:42:19 CST** — suite-specific object checkpoint
> 已完成 25/25 下载并通过 CPU-only 静态校验：无 `.incomplete`，四个模型分片
> 共 982 个张量且全部可读，LoRA、action head、proprio projector 均可解析，
> normalization key 只有正确的 `libero_object_no_noops`。目录约 15 GB，校验后
> 磁盘剩余 296 GB。详细 SHA-256 见 `docs/CHECKPOINT_OBJECT_20260720.md`。
> 尚未准备或启动 object GPU 评测。

> **状态更新：2026-07-20 13:45:18 CST** — object 专用启动器
> `eval/launch_oft_object_robot398_tmux.sh` 已准备并通过 CPU-only dry-run。
> CUDA 明确不可见；398/398 官方 object Robot-init 任务严格分为
> `57×6 + 56`，无重复、无遗漏；七条命令均绑定 object checkpoint、seed 7、
> OSMesa 和物理 GPU 1--7。dry-run 后确认无输出目录、无
> `ra_oft_object_robot398` tmux 会话、无 evaluator 进程。真实 GPU 评测尚未
> 启动。

> **状态更新：2026-07-20 13:47:01 CST** — 经用户明确确认，object
> Robot-init 398 条正式评测已启动。tmux 会话为
> `ra_oft_object_robot398`，七个窗口为 `shard0_gpu1` 到 `shard6_gpu7`，
> 分片任务数为 `57×6 + 56`，延迟 0--180 秒启动，无 evaluator 超时。启动前
> GPU 1--7 均为 0% 利用率、18 MiB 基础占用且无计算进程；GPU 0 未使用。
> 首片已完成四个 checkpoint shard 的读取并进入模型初始化，无立即错误。
> 输出根目录为 `logs/openvla_oft_object_robot_full398_7gpu_20260720`。当前运行中，
> 尚无最终成功率。

> **状态更新：2026-07-20 20:34:44 CST（评测实际于 14:47:13 完成）** —
> suite-specific object Robot-init 正式评测验收通过：398/398、missing=0、
> duplicate=0，成功 `137/398 = 34.4221%`。七片均 exit code 0、元数据一致，
> 无 evaluator 残留，GPU 1--7 已释放。相同任务上的 OFT_m 为
> 83/398（20.8543%），本次提升 13.5678 个百分点、净增 54 个成功任务。
> 详细记录和 SHA-256 见 `docs/BASELINE_OFT_OBJECT_ROBOT398_20260720.md`。

> **状态更新：2026-07-20 20:39:19 CST** — 开始下载 suite-specific
> `libero_goal` checkpoint。Hugging Face 仓库为
> `moojink/openvla-7b-oft-finetuned-libero-goal`，目标目录为
> `/home/imc/models/ra-loop/openvla-oft-goal`，共享 tmux 会话为
> `ra_download_goal`。启动前目标目录与 tmux 名称均不存在，磁盘剩余 296 GB；
> 下载已正常进入 25 文件获取阶段。当前只进行网络和磁盘写入，不使用 GPU，
> 尚未启动 goal 评测。

> **训练主线审计更新：2026-07-20** — 已完成 RIPT-VLA / RA-LOOP CPU-only
> readiness audit，未启动 GPU。当前训练脚本不能安全执行：缺少原版 LIBERO
> HDF5 demos 和 spatial Laplace scale header；workspace `config/paths.yaml` 缺失，
> upstream paths 指向无效 `/storage`；vanilla 脚本错误地加载已训练 RIPT adaptor；
> `code.ra_optimizer` 与 Python 标准库冲突而不可导入；现有 patch 与当前 upstream
> factory 入口不匹配；camera/light、anchor 和跨扰动 consistency 语义尚未实现
> 正确。详细证据与安全修复顺序见 `docs/TRAINING_AUDIT_20260720.md`。下一步先修复
> 数据、scale header、路径和 bounded vanilla dry-run，不直接启动训练。

> **训练资产下载更新：2026-07-20 21:05 CST** — 已在共享 tmux
> `ra_spatial_assets` 启动训练准备资产下载（不使用 GPU）。
> `LIBERO_SPATIAL_scale_header.pth` 已完整下载，大小 335,805,208 bytes；
> 官方 `libero_spatial` HDF5 共 10 个、总计 6,237,050,764 bytes，正在下载到
> `/home/imc/data/ra-loop/libero-datasets/libero_spatial/`。下载结束后只做文件数、
> 大小和 HDF5 可读性检查，再单独确认 bounded vanilla LOOP dry-run。
>
> 同次复核发现 `ra_download_goal` 已退出但 Goal checkpoint 尚不完整：缺少
> `model-00002-of-00004.safetensors`，缓存中保留约 4.09 GB 的 `.incomplete`
> 文件。该目录当前不可用于评测；后续用原命令续传，不删除缓存。

> **训练资产验证：2026-07-20 21:22 CST** — Spatial 训练资产只读验证全部
> 通过。scale header 大小为 335,805,208 bytes，SHA-256 为
> `388c491761523d41a28f62b2262d52611c98c1cb89dae09a7290dc1a1c1097b8`；
> 10/10 个 HDF5 均能由 `h5py` 打开，文件集合和逐文件大小匹配官方清单，
> `/data` 下共 500 条 demo、62,250 个非空 action steps，且每条 demo 均包含
> `actions` 与 `obs`。可复跑检查脚本为
> `scripts/verify_spatial_training_assets.py`。本步骤未使用 GPU。

> **Vanilla LOOP smoke 配置：2026-07-20** — 已新增带安全闸的启动器
> `train/vanilla_loop_spatial_smoke.sh` 并完成 CPU-only Hydra compose。
> 默认执行只打印 resolved config，不初始化 CUDA；只有显式使用
> `--run <GPU_ID>` 才会启动训练。resolved config 已确认使用 suite-specific
> Spatial checkpoint、已验证的 Spatial scale header 和
> `libero_spatial` HDF5；规模限制为单任务、1 demo batch、K=2、1 个训练步、
> 单环境、最长 10 个环境步，关闭 dynamic sampling、W&B、训练后评测和
> checkpoint 保存。shell 语法与三类输入文件存在性检查通过。本步骤未使用 GPU。

> **首次 GPU smoke 诊断与 LIBERO 隔离：2026-07-20 21:34 CST** — 首次启动
> 未执行任何训练步。先发现 `~/.local` 包覆盖 conda 环境，已在启动器加入
> `PYTHONNOUSERSITE=1`，确认使用 conda 内 `peft 0.11.1` 与
> `transformers 4.40.1`。随后 dataset 构建失败，因为原配置导入
> LIBERO-Plus，其 `libero_spatial` 注册了 2402 个扰动任务，不包含官方原始
> 10 任务短名。现已将训练侧隔离到干净官方仓库
> `/home/imc/code/LIBERO-official`（commit
> `8f1084e3132a39270c3a13ebe37270a43ece2a01`），新增
> `.libero_official/config.yaml`，并由 smoke 启动器局部设置 `PYTHONPATH` 和
> `LIBERO_CONFIG_PATH`。CPU 验证确认 benchmark 为 10 任务、所选任务存在，
> 且 10/10 demo 路径均对应已下载 HDF5；LIBERO-Plus 仍保留给鲁棒评测。
> GPU 7 已释放，无 LoRA/checkpoint 输出。
>
> RIPT 模型加载器在失败运行中曾自动更新 suite-specific checkpoint 的
> `config.json`，并保留备份
> `config.json.back.20260720_213355`；权重未修改。再次启动前应先比较备份，
> 并为可变模型元数据建立隔离 runtime mirror，避免继续写原 checkpoint。

> **Checkpoint runtime mirror：2026-07-20** — 已比较原目录的 `config.json`
> 与 `config.json.back.20260720_213355`，二者 SHA-256 均为
> `bcb688a66f3e94a42311b77d37b7e1484886701f7b2aa8e36d165dfccac92ae6`，
> 内容完全相同，因此不执行恢复或删除。已创建
> `runtime/openvla-oft-spatial-smoke`：`config.json`、
> `modeling_prismatic.py`、`configuration_prismatic.py` 为独立副本，其余模型
> 文件（包括 4 个 safetensors shard）均为指向原 checkpoint 的符号链接。
> mirror 实际新增占用 184 KB，并已加入 `.gitignore`；smoke resolved config
> 已确认只指向 mirror。可复建脚本为
> `scripts/prepare_openvla_spatial_runtime_mirror.sh`，默认拒绝覆盖已有 mirror。
> 后续上游自动生成 config backup 或重写 auto_map 均只发生在 runtime mirror。

> **run3 dataset 路径修复：2026-07-20 21:43 CST** — GPU smoke 在训练前的
> dataset 构建阶段退出，未执行训练步。官方 LIBERO 隔离已生效（日志为
> `task_idx_to_use [8]`），但 RIPT `build_dataset` 同时拼接
> `task.suite_name` 和官方 `get_task_demonstration()` 返回的
> `libero_spatial/...`，形成重复路径
> `libero_spatial/libero_spatial/*.hdf5`。已仅覆盖
> `task.dataset.suite_name=.`，实验目录的 `task.suite_name=libero_spatial`
> 保持不变。新增 CPU-only 预检
> `scripts/preflight_vanilla_spatial_dataset.py`，以同一组参数实际构建并读取
> dataset；结果为 1 条 sequence、action shape `(600, 7)`、`task_id=0`，
> 任务注册、路径、HDF5 解析和样本读取全部通过。GPU 7 已释放并保持空闲；
> 本轮未自动重启。

> **run4 RIPT factory 兼容修复：2026-07-20 21:47 CST** — run4 已通过
> 模型、scale header、CLIP、dataset 和单任务环境创建，但在训练前构建
> optimizer 时退出；未执行训练步。根因是 pinned RIPT commit `440990e` 的
> `train_ript_openvla_oft.py` 将 `enable_rollout_stats_tracking` 误传给
> `RLOptimizerOpenVLAOFT`，而该参数实际属于 `RolloutGenerator`。未修改上游
> 工作树；新增项目内
> `ra_loop.ript_compat.RLOptimizerOpenVLAOFTCompat`，接收误传参数并设置到已传入
> 的 rollout generator，Hydra target 由 smoke 启动器局部覆盖。直接构造和与
> 上游一致的 Hydra `_partial_` 两阶段构造测试均通过（2 passed），resolved
> config 已确认指向兼容类。CLIP 605 MB 权重已缓存；GPU 7 已释放。

> **run5 rollout 子进程诊断：2026-07-20 21:54 CST** — 兼容层已生效，run5
> 首次成功进入 `Starting training loop`、读取训练 batch 并调用 K-sampling
> rollout；在环境 `reset()` 前的子进程初始化退出，未产生 episode、未执行
> PPO。根因是官方 `SubprocVectorEnv` 的 spawn worker 继承 torchrun 的
> `WORLD_SIZE/RANK/MASTER_PORT`，worker 反序列化 runner 时导入 OpenVLA，
> Accelerate 因此误启动第二个 distributed store 并与主进程端口冲突，随后父
> 进程收到 `ConnectionResetError`。GPU 7 已释放。单环境 bounded smoke 的
> 安全修复应为项目内 runner 子类使用官方 `DummyVectorEnv`（in-process），
> 不修改上游和系统端口。另确认当前 `algo.rollouts_per_env=1` 与 K=2 不一致；
> 下一轮应设为 2，以单环境顺序生成两条 episode 后再计算 leave-one-out。

> **run6 前 in-process runner 修复：2026-07-20** — 已在项目内新增
> `InProcessOpenVLAOFTLiberoRunner`，仅允许 `num_parallel_envs=1`，并使用
> 官方 LIBERO `DummyVectorEnv`/`DummyEnvWorker` 创建环境，避免 torchrun
> rendezvous 环境变量进入 spawn worker；未修改 RIPT/LIBERO 上游。smoke
> 配置同步改为 `rloo_batch_size=2`、`rollouts_per_env=2`，以单环境顺序生成
> 两条 episode。CPU 测试确认 optimizer 参数转发、Hydra 两阶段 factory、
> in-process runner 类型与无新增 child process 均通过（3 passed）；resolved
> config 确认 runner/optimizer 均指向项目内兼容类、单环境、K=2、2 rollouts、
> 1 training step。本步骤未使用 GPU。

> **Vanilla LOOP bounded smoke 通过：2026-07-20 22:00 CST** — run6 在 GPU 7
> 完整执行并正常退出：进入 training loop，单环境顺序完成 K=2 两条 rollout
>（14.68 s），完成 1 个 PPO epoch / 2 个 PPO batch，打印 metrics，关闭环境并
> 出现 `[info] Finished training`；GPU 随后释放。两条 horizon-10 rollout 均
> 失败，因此 reward、leave-one-out advantage、loss 和 gradient norm 均为 0，
> 这是连通性 smoke 的预期，不作为学习有效性证据。详细配置、指标与兼容修复
> 见 `docs/VANILLA_LOOP_SPATIAL_SMOKE_20260720.md`。下一阶段可准备较真实 horizon
> 的 vanilla 学习验证；RA-LOOP 实现仍须先完成语义重构和单元测试。

> **Vanilla 学习信号 probe 准备：2026-07-20** — 已新增安全启动器
> `train/vanilla_loop_spatial_learning_probe.sh`；默认仅做 CPU Hydra compose，
> GPU 仍需显式 `--run <GPU_ID>`。probe 保持单任务、单 GPU、单训练步和
> in-process 单环境，但使用真实 Spatial horizon=220、K=4、
> `rollouts_per_env=4`、4 demos、固定 dataloader 顺序，关闭 dynamic sampling、
> W&B、额外评测和 checkpoint 保存。CPU dataset 预检实际构建 4 条 sequence，
> 首样本 action shape `(600, 7)`；resolved config 全部匹配。原 K=2/horizon=10
> smoke profile 复核不变。probe 的判据为 4 条 episode 完成、PPO 正常退出；
> 若 reward 同时包含 0/1，应出现非零 advantage/gradient，若四条 reward 全同，
> 则运行有效但学习信号结论不充分，不能据此声称训练改善。

> **Vanilla mixed-reward 学习信号 gate 通过：2026-07-20 22:35 CST** —
> `mixed_reward_probe` 在 GPU 7 完整执行并正常退出。单任务、单训练步、
> horizon=220、K=8、scale=5 的 8 条顺序 rollout 得到奖励
> `[1,0,1,1,0,1,0,0]`，即 4/8 成功、mean reward=0.5；
> `non_zero_adv_ratio=1.0`、PPO loss、model gradient 和 header gradient 均非零，
> 因而本地 OpenVLA-OFT + RIPT vanilla LOOP 的真实学习信号链路已经验证。
> rollout 用时 108.83 s，PPO 用时 57.67 s，总计 166.85 s，最终出现
> `[info] Finished training` 并释放 GPU。该运行关闭 checkpoint 保存，不产生模型
> 成果，也不能证明性能提升。`gradient_norm_header_stats=85.5` 与
> `pg_clipfrac_stats=0.3961` 需要在长训练前审计指标/裁剪语义。详情见
> `docs/VANILLA_LOOP_SPATIAL_MIXED_REWARD_PROBE_20260720.md`。下一步转入
> RA-LOOP 目标语义重构和 CPU 单元测试，不继续扩大 vanilla GPU 训练。

> **RA-LOOP 目标语义审计完成：2026-07-20** — 当前
> `code/ra_optimizer.py` 不可进入 GPU：除导入/Factory 过时、camera/light no-op、
> anchor 误标和硬编码 state slice 外，更关键的是 Robot-init 改变物理状态，正确
> 动作本来可能不同，不应直接施加动作一致性惩罚。首个 RA pilot 因此收窄为
> Spatial Robot-init recovery-only：真实 anchor + 具名 Panda joint qpos 扰动，
> `lambda_consistency=0`，并拒绝尚未实现的 camera/light/layout。HDF5 只读检查
> 证实 demo state 为 92 维，现有 `[:7]`/index 9 假设会触碰错误自由度。
> 同时确认 probe 的 header grad 85.5 是裁剪前 norm（实际执行 1.0 clip），
> clipfrac 0.3961 不是报错但提示逐 episode 更新过强。详细实施契约见
> `docs/RA_LOOP_SEMANTIC_AUDIT_20260720.md`。本步未使用 GPU、未写模型/数据；
> 下一步才是按契约实现纯 CPU 核心与单元测试。

> **Robot-init recovery-only CPU 核心完成：2026-07-20 22:44 CST** — 已新增
> `ra_loop/robustness.py` 与 `tests/test_robustness.py`：真实 anchor/扰动配对、
> 具名 `robot0_joint1..7` qpos 地址与关节限位验证、确定性 Gaussian 扰动、
> 非目标 state 不变、真实 applied metadata、unsupported perturbation fail-closed、
> recovery reward、valid-mask 分项统计及 `lambda_r=0` 精确 vanilla 退化均已实现。
> 核心测试 12/12 通过；在 `ript_vla_openvla_oft` 环境、官方 LIBERO 隔离路径、
> `/tmp` Numba cache 且隐藏 CUDA 的项目全量回归 15/15 通过。本步未连接 Hydra/
> RIPT rollout，未创建 simulator，未使用 GPU，未修改 Conda、上游、模型或数据。
> 详情见 `docs/RA_LOOP_CPU_CORE_20260720.md`。下一步是 live official-LIBERO
> 具名关节只读/CPU 验证和 exactly-once RIPT adapter 测试；通过后才准备 GPU。

> **Live official-LIBERO joint gate 通过：2026-07-20 22:49 CST** — 在单个
> Spatial CPU/OSMesa 环境中确认 MuJoCo Panda qpos 地址为 0..6，而 LIBERO 的
> 92 维 `MjSimState.flatten()` 首位为 simulation time，因此实际 init-state 地址
> 必须显式映射为 1..7。核心已加入可验证的 `state_qpos_offset=1`，避免扰动
> state[0]。`0.001 rad` 确定性扰动实际只改变 1..7，输入数组未变，七个真实
> joint limit 均读取成功，模拟器 set/get state round-trip 通过。修正后核心测试
> 13/13、项目全量回归 16/16 通过。本步未加载 OpenVLA、未执行 rollout、未用
> GPU、未写模型或数据。详细结果见
> `docs/RA_LOOP_LIVE_JOINT_VALIDATION_20260720.md`。下一步仅实现/test
> exactly-once RIPT adapter，仍不启动 GPU。

> **Exactly-once RIPT rollout adapter 通过：2026-07-20 22:54 CST** — 已新增
> `ra_loop/ript_recovery.py` 和 `tests/test_ript_recovery.py`。adapter 仅在一次
> upstream `generate_rollouts()` 调用期间包装 `run_policy_in_env`，注入真实
> anchor/Robot-init pair 并标记实际 metadata；成功、异常和输入拒绝路径均在
> `finally` 恢复原方法。强制单 in-process env、even K、runner rollout=K、K 个
> identical base state；random/mixed init、subprocess/parallel env、缺少 episode
> 均 fail-closed。假 rollout adapter 测试 6/6、项目回归 22/22、py_compile 通过。
> 本步未创建 live env、未接 Hydra、未加载模型或 GPU，未修改上游/Conda/模型/
> 数据。详情见 `docs/RA_LOOP_RIPT_ROLLOUT_ADAPTER_20260720.md`。下一步仍为
> CPU-only：接 recovery reward/optimizer，确保 upstream PPO 也只调用一次、
> valid mask 正确且 success 指标不被 augmented reward 污染，然后再 compose Hydra。

> **Recovery reward/optimizer adapter 通过：2026-07-20 22:58 CST** — 已在
> `ra_loop/ript_recovery.py` 增加 `RobotInitRecoveryReward` 与
> `RobotInitRecoveryOptimizer`。reward 仅对“真实 applied 且成功”的 Robot-init
> 加 `lambda_r`；optimizer 捕获同一批 rollout 后只调用一次 upstream PPO，保留
> 原 advantage/backward/clip/step 路径，并在 `finally` 恢复 generator。valid-mask
> padding 不进入统计；`mean_scores`/task success 被修正为二元成功率，增广奖励
> 单独记录为 `mean_R_total`/`mean_rlhf_reward`，anchor/perturbed success 分开记录。
> 多 GPU 指标暂时 fail-closed，首个 pilot 仅允许单 GPU。Recovery adapter 测试
> 10/10、项目回归 26/26、py_compile 通过。本步未 compose Hydra、未创建 env、
> 未加载模型/GPU，未修改上游/Conda/模型/数据。详情见
> `docs/RA_LOOP_RECOVERY_OPTIMIZER_20260720.md`。下一步仅做 bounded Spatial
> CPU Hydra compose 与禁用 env creation 的 factory 实例化。

> **Bounded Spatial RA Hydra/factory gate 通过：2026-07-20 23:02 CST** — 新增
> `train/ra_loop_spatial_preflight.sh`（刻意无 GPU 模式）与
> `scripts/preflight_ra_hydra_factories.py`。基于已验证 Spatial upstream config
> 成功解析四个项目 target：in-process runner、Robot-init rollout、recovery
> reward、recovery optimizer；配置为单任务/单步/K=4（2 pairs）/单 env/h10/
> strength 0.001 rad/seed 20260720/lambda_r 0.5/PPO1/scale2，W&B、周期 eval 和
> 实质 checkpoint 保存均关闭。`create_env=false` 的 reward/rollout/optimizer
> factory 实例化通过，misrouted stats flag 也正确转发；未加载 dataset/env/model/
> checkpoint。CUDA 被显式隐藏，TensorFlow 的 `CUDA_ERROR_NO_DEVICE` 只是探测
> 结果，未占用显卡。最终项目回归 27/27、py_compile 通过。详情见
> `docs/RA_LOOP_HYDRA_PREFLIGHT_20260720.md`。下一步先生成独立 GPU launcher 并
> 只检查命令；再次确认后才启动 K4/h10 的一步 connectivity smoke。

> **最小 RA-LOOP GPU launcher 静态审查通过：2026-07-20** — 已新增
> `train/ra_loop_spatial_connectivity_smoke.sh`，本步仅执行 `bash -n` 和默认
> `--print-command`，未查询/初始化 GPU。launcher 无参数只打印；真正执行必须
> 显式 `--run <0..7>`。其 45 个核心 override 与通过的 CPU preflight 完全一致，
> 仅实验名/输出路径不同；固定单任务/单步/K4/2 pairs/单 env/h10/0.001 rad/
> lambda_r 0.5/PPO1/scale2，W&B、eval、保存关闭，无 timeout。运行路径先验证
> 文件，再查询所选卡；显存 >1 GiB、util >10% 或温度 >75C 将拒绝启动，之后
> 才暴露 CUDA。详情见 `docs/RA_LOOP_GPU_LAUNCHER_REVIEW_20260720.md`。下一步在
> 用户再次确认后检查空闲卡，并在共享 tmux 中启动；本 smoke 只验证连通性，
> 不以 h10 的 reward/学习改善为目标。

> **首个 RA-LOOP GPU connectivity smoke 通过：2026-07-20 23:15 CST** — 在
> 空闲物理 GPU 7（启动前 18 MiB/0%/41C）通过共享 tmux
> `ra_loop_spatial_smoke:run1` 启动，单任务/单步/K4/2 pairs/单 env/h10/
> 0.001 rad/lambda_r 0.5 配置完整跑完，出现 `[info] Finished training` 和
> `[RA_LOOP_EXIT] 0`；GPU 随后回落到 18 MiB/0%。四条 h10 rollout 全失败，故
> reward/advantage/loss/gradient 均为 0，符合 connectivity smoke 预期；关键
> RA 指标 `valid_anchor_count=2`、`valid_perturbed_count=2`、
> `lambda_r_effective=0.5` 均出现，exactly-once rollout/reward/PPO/metric/cleanup
> 链路已验证。rollout 44.65 s、PPO 2.99 s、总 step 48.01 s。输出目录无文件或
> checkpoint；仅保存项目日志和 runtime `config.json` metadata backup，原权重
> 未修改。详情见 `docs/RA_LOOP_SPATIAL_CONNECTIVITY_SMOKE_20260720.md`。下一步
> 不直接放大 GPU 训练，先 CPU-only 标定 LIBERO-Plus Robot-init 的真实 joint
> shift，再确定 h220/K8 learning-signal probe 的扰动强度。

> **LIBERO-Plus Robot-init CPU 标定完成：2026-07-20 23:24 CST** — 对首个
> Spatial 任务的 37 个 Robot Initial States 变体完成纯源码/元数据标定；未导入
> LIBERO/robosuite/MuJoCo、未创建模拟器、未使用 GPU、未修改 Plus 数据。Plus
> `initstate_N` 实际选择 `PandaN`，编号 1--100 到 401--500 的 7 关节偏移 L2
> 半径严格为 0.1--0.5 rad；该生成半径与 classification JSON 的
> `difficulty_level` 是不同标签，不能互换。原任务 50 条 `.pruned_init` 的
> state[1:8] 相对 canonical Panda qpos 的 L2 中位数仅 0.0506 rad。当前 RA
> `strength` 经代码复核是逐关节 Gaussian 标准差，不是总 L2；若保持现实现，
> `strength=0.0392 rad/joint` 的期望 7D L2 约为 0.1 rad，但更可解释的下一步是
> 先增加 Plus 对齐的 fixed-L2 采样模式。
>
> 同步静态审计发现现有 bounded baseline evaluator 每个变体执行
> `env.reset()` 后，又将相同基础 `.pruned_init[0]` 通过 Plus
> `sim.set_state_from_flattened()` 写回，因而会覆盖 PandaN reset 时的 robot qpos。
> 所以已记录的 Spatial Robot 135/350=38.57% 仍是分类表 350/350 覆盖完整的
> 可复查结果，但不能证明 PandaN 物理偏移在 rollout 中实际保留。RA adapter
> 直接修改送入 rollout 的 flattened state[1:8]，不受该覆盖路径影响。改 evaluator
> 或重跑 baseline 前，先另做一个 CPU/live reset 前后 qpos 读回 gate，并由用户
> 确认评测口径。本步骤标定不变量通过；official-LIBERO 隔离环境项目回归
> 27/27 通过。报告、结构化数据和可复跑脚本分别为
> `docs/LIBERO_PLUS_ROBOT_INIT_CALIBRATION_20260720.md`、
> `docs/LIBERO_PLUS_ROBOT_INIT_CALIBRATION_20260720.json` 和
> `scripts/calibrate_libero_plus_robot_init.py`。

> **RA-LOOP 已到训练前一步：2026-07-20 23:33 CST** — 已实现 Plus 对齐的
> `fixed_l2` Robot-init 采样，同时保留历史 `gaussian_std` 默认语义；新 metadata
> 明确记录 sampling mode 和关节限位裁剪后的实际 L2。新安全启动器
> `train/ra_loop_spatial_learning_probe.sh` 已锁定为单任务、单 GPU/单环境、
> 1 step、K8（4 anchor + 4 perturb）、h220、fixed-L2 0.1 rad、seed 20260720、
> lambda_r 0.5、scale 5、4 demos、无 W&B/periodic eval/checkpoint save。
> 无参数 CPU-only Hydra/factory preflight 和 `--print-command` 均通过；
> `create_env=false`，未加载 dataset/env/model，未查询/使用 GPU，且 probe 输出目录
> 不存在。fixed-L2 核心 16/16、official-LIBERO 隔离环境项目回归 30/30 通过。
> 当前尚未启动训练；下一动作就是经用户确认后在共享 tmux 执行该 h220/K8
> learning-signal probe。详细就绪记录见
> `docs/RA_LOOP_TRAINING_READY_20260720.md`。

> **RA h220/K8 probe 已启动：2026-07-20 23:44 CST** — 经用户确认，在启动前
> 18 MiB/0%/41°C 的物理 GPU 7 上启动；共享 tmux 为
> `ra_loop_spatial_learning_probe:run1`，日志为
> `logs/ra_loop_spatial_learning_probe_20260720/run1.log`。模型、scale header、
> dataset、单环境均初始化完成，已进入 8 条 rollout，第 1 条 anchor 成功并开始
> 第 2 条；观察时 GPU 约 18.1/24.6 GB、47°C，无报错。按上次 K8/h220 的
> 166.85 秒训练循环实测，加上初始化预计本次总计约 4--6 分钟。当前尚未完成，
> 尚不能报告最终 reward、advantage 或梯度。

> **RA h220/K8 learning-signal gate 通过：2026-07-20 23:55:40 CST** — 8 条
> rollout 为 `[1,0,1,0,0,1,1,0]`，总成功 4/8；anchor 3/4=75%，fixed-L2
> perturb 1/4=25%。`mean_R_success=0.5`、`mean_R_recovery=0.125`、
> `mean_R_total=0.5625`、`non_zero_adv_ratio=1.0`，PPO loss 和模型梯度非零；
> 8/8 PPO batch 完成，出现 `[info] Finished training` 和 `[RA_LOOP_EXIT] 0`。
> rollout 622.39 秒、PPO 60.23 秒、训练循环共 683.00 秒（11分23秒）。结束后
> GPU 7 已回到 18 MiB/0%/41°C。按配置没有 checkpoint 文件。该结果证明
> recovery-only RA 链路具备真实学习信号，但不证明性能提升。`pg_clipfrac=0.427`
> 和裁剪前模型 gradient norm 1.97 提示下一步应先做保守、可保存、可中途评测的
> 短程 pilot，不直接扩大成长训练。详情见
> `docs/RA_LOOP_SPATIAL_LEARNING_PROBE_20260720.md`。

> **保守 overnight pilot 训练前检查完成：2026-07-21 00:10 CST** — 基于已通过
> 的 fixed-L2 learning-signal gate，准备单任务、单 GPU、21 steps、每步 K8/h220、
> fixed-L2 0.1、lambda_r 0.5、scale 5 的保存型 pilot；为缓解 probe 的高
> clipfrac，model/action-header LR 从 5e-5 降到 1e-5。每 5 步保存，预期产生
> step 5/10/15/20 四份 LoRA+headers。W&B 和 periodic eval 关闭，输出必须全新，
> 启动至少要求 20 GB 可用空间。CPU Hydra/factory preflight、shell/命令检查和
> 项目回归 30/30 均通过；输出/日志/tmux 名称启动前不存在，GPU 7 为
> 18 MiB/0%/41°C。按 683 秒/step 估计约 4 小时。上游 checkpoint 不含 optimizer/
> scheduler state，中断后只能从最近 adapter/header 作为新 run 初始化，不能精确
> 续步。详细计划见 `docs/RA_LOOP_OVERNIGHT_PILOT_20260721.md`。

> **RA-LOOP overnight pilot 已启动：2026-07-21 00:11 CST** — 经用户确认，
> 在空闲 GPU 7 通过共享 tmux `ra_loop_spatial_overnight_pilot:run1` 启动。日志为
> `logs/ra_loop_spatial_overnight_pilot_20260721/run1.log`。21 steps、K8/h220、
> fixed-L2 0.1、LR 1e-5、save interval 5 和 270 GB 可用空间已由运行日志确认；
> 模型、scale header、4-demo dataset、单环境均初始化完成，当前已进入 step 1/21
> 的 episode 1/8，无立即错误。模型报告 allocated 15.75 GB、reserved 17.69 GB。
> 按单步 683 秒估算约 04:10 完成，保守范围 03:45--05:15；预期 step 5/10/15/20
> 保存四份 checkpoint。

> **RA-LOOP 21-step pilot 完成：2026-07-21 03:41:27 CST** — 21/21 steps、
> 每步 K8 rollout/PPO 完整执行，训练循环共 3小时29分25秒，出现
> `[info] Finished training` 与 `[RA_LOOP_EXIT] 0`，GPU 7 已回到
> 18 MiB/0%/41°C。21 步在线采样总成功 112/168=66.67%，anchor
> 58/84=69.05%，fixed-L2 perturb 54/84=64.29%；21/21 步
> `non_zero_adv_ratio=1.0`。first-5 到 last-5 的在线成功率从 55.0% 到 67.5%，
> 但样本/策略在变化，不能视作独立性能提升。平均 pg clipfrac 0.075，较单步 probe
> 下降；仍有一次 pg ratio 3.09 和裁剪前 gradient norm 8.95 离群，故不继续扩大
> 训练。step 5/10/15/20 四份 checkpoint 均存在，每份 879 个 LoRA tensor、
> action header 16 tensors、scale header 18 tensors，CPU 打开通过并已记录
> SHA-256。下一步先在相同种子/初始状态下比较四个 checkpoint 的 anchor 与正确
> fixed-L2 Robot-init，不依据训练期末值直接选最后一个。详细见
> `docs/RA_LOOP_OVERNIGHT_PILOT_RESULT_20260721.md`。

> **五候选独立 paired-eval 已准备：2026-07-21 09:18:43 CST** — 新增 step
> 0/5/10/15/20 的统一评测：每个候选使用完全相同的 init indices 0--9，运行
> 10 anchor + 10 fixed-L2 0.1 Robot-init，perturb seeds 固定为
> 20260720--20260729，动作使用 deterministic mean，单环境 h220。总计 100 条；
> GPU 1--5 并行预计 25--35 分钟。RIPT policy 的保存型 LoRA+
> `openvla_headers.pt` 加载路径已核对；step 0 使用零增量初始化作 base 对照。
> evaluator 使用 `.incomplete` 后原子完成、拒绝覆盖、记录 checksum；launcher
> 具有显存/利用率/温度闸。Python/shell、五套 CPU plan、五条命令和项目回归
> 30/30 全部通过，输出根目录不存在。本步骤 CUDA 隐藏，未使用 GPU。详细见
> `docs/RA_LOOP_CHECKPOINT_EVAL_READY_20260721.md`。下一步再次确认后检查 GPU
> 1--5 并行启动。

> **五候选独立 paired-eval 已启动：2026-07-21 09:39:17 CST** — 共享 tmux
> `ra_loop_checkpoint_pair_eval` 已运行五个窗口：step 0/5/10/15/20 分别使用
> GPU 1/2/3/4/5，共 100 episodes。启动前五卡均约 18 MiB、0%、41--43°C；
> 启动后检查约 15.8--16.7 GiB、44--55°C。step 5/10/15/20 均已确认
> base model、对应 LoRA 与 headers 成功加载并进入首个 episode；step 5 检查时
> 已完成 2/20。step 0 正常运行但最初 stdout 受管道缓冲影响，后续窗口 launcher
> 已加入 `PYTHONUNBUFFERED=1` 以实时显示 episode。结果完成前不继续训练；下一步
> 验收五份 20/20 原子结果并做 paired checkpoint 选择。详细见
> `docs/RA_LOOP_CHECKPOINT_EVAL_READY_20260721.md`。

> **五候选独立 paired-eval 已完成：2026-07-21 09:55:34 CST** — 五进程
> 均 exit 0，100/100 行完整且无 `.incomplete`；每个 step 都是 20 个唯一
> `(mode, init)`，fixed-L2 范数均为 0.1，seed 完整覆盖 20260720--20260729。
> step 0 为 anchor 9/10、perturbed 10/10，唯一失败是 anchor init 4；step
> 5/10/15/20 均为 anchor 10/10、perturbed 10/10。结果支持 pilot 未退化且
> step 5 已达到本组样本上限，但单任务 10 对样本已经饱和，无法区分四个训练后
> checkpoint，也不能直接和论文完整 suite 数字比较。暂以 step 5 为保守候选，
> 继续训练前先扩大独立任务/初态评测。详细见
> `docs/RA_LOOP_CHECKPOINT_PAIR_EVAL_RESULT_20260721.md`。

> **三任务短评测完成并推进至下午训练前：2026-07-21 12:50:22 CST** — 在
> GPU 1--6 并行比较 step 0/5 的三个 unseen Spatial tasks，各 6 anchor + 6
> fixed-L2 0.1，总计 72/72 episodes、6/6 exit 0，耗时 14分59秒。step 0 与
> step 5 结果完全相同：anchor 18/18、perturbed 15/18、总计 33/36，三个失败
> pair 也一致；说明 step 5 无跨任务退化，但未显示跨任务提升。已准备 4-task、
> 35-step、K8/h220、LR1e-5、step-5 warm-start 的下午训练，四任务 DataLoader
> 强制 `shuffle=true`，每 5 steps 保存，预计 6--8 小时。CPU preflight 与安全
> shared-tmux launcher 已准备，训练尚未启动；等待用户再次确认。详见
> `docs/RA_LOOP_DISAMBIGUATION_EVAL_20260721.md` 和
> `docs/RA_LOOP_AFTERNOON_TRAINING_READY_20260721.md`。

> **下午四任务 RA-LOOP 已启动：2026-07-21 12:56:59 CST** — 经用户确认，
> 35-step 训练已在 GPU 7、tmux `ra_loop_spatial_afternoon_multitask:run1`
> 启动。step-5 LoRA 与 headers、四任务各 4 demos、四个 in-process env 均已
> 成功加载，进入 step 1/35 的第 1/8 rollout；检查显存约 18.4 GiB、46°C。
> 预计约 6--8 小时，每 5 local steps 保存 checkpoint。日志为
> `logs/ra_loop_spatial_afternoon_multitask_20260721/run1.log`。

> **下午四任务 RA-LOOP 已完成：2026-07-21 18:58:18 CST** — 35/35 updates，
> 训练循环 6小时00分29秒，`Finished training`、exit 0，GPU 7 已释放。280 条
> 在线 rollout 总成功 188/280=67.14%，anchor 101/140=72.14%，perturbed
> 87/140=62.14%；四任务均被 shuffle 覆盖，top-drawer 最难（29/64）。平均
> pg clipfrac 0.0638、pg ratio 1.0024，未数值发散。六份 step
> 5/10/15/20/25/30 checkpoint 均通过 tensor 级读取。上游 0-based 保存语义导致
> step 30 对应第 31 次更新，最后 4 次更新未保存；不存在 step 35，独立评测不得
> 假设终点模型已落盘。下一步评测六个已保存 checkpoint，不依据在线成功率选型。
> 详见 `docs/RA_LOOP_AFTERNOON_TRAINING_RESULT_20260721.md`。

> **下午六 checkpoint 独立评测已启动：2026-07-21 20:18 CST** — step
> 5/10/15/20/25/30 分别在 GPU 1--6 并行，每份运行四任务各 6 anchor + 6
> fixed-L2 0.1，共 288 episodes；init/seed 与训练前 baseline 精确匹配。
> 24 个 CPU plans、语法和 30/30 tests 通过。六份 LoRA/headers 均已加载并进入
> 首个 episode；启动后约 16.7 GiB、45--52°C。预计 45--60 分钟，完成前不将
> 中间结果视为有效。详见 `docs/RA_LOOP_AFTERNOON_CHECKPOINT_EVAL_20260721.md`。

> **下午六 checkpoint 独立评测已完成：2026-07-21 21:04 CST** — 六进程
> 全部 exit 0，288/288 episodes、24/24 task summaries 完整，无 `.incomplete`，
> GPU 1--6 已释放。训练前 warm-start 基线为 anchor 24/24、fixed-L2 21/24、
> 总计 45/48；训练后最佳 step 5/15 均为 44/48，其余为 43/48，没有 checkpoint
> 超过基线。step 5 修复一个 stove 扰动 pair，但在原任务退化两个 pair；step 15
> 只有一个 anchor regression，是最保守训练后候选，但不替代 baseline。当前配置
> step 20--30 无改善，下一步转向 anchor 回归保护与目标失败 pair 消融，不直接延长
> 训练。详见 `docs/RA_LOOP_AFTERNOON_CHECKPOINT_EVAL_RESULT_20260721.md`。

> **reward interference 已定位：2026-07-21** — 上游把四条 anchor 与四条
> perturbed reward 放在同一个 K=8 RLOO baseline 中，而当前成功 reward 分别为
> 1.0 与 1.5。历史 35-step 日志严格复算显示，10/35 steps、共 39 条已经成功的
> anchor 收到负 advantage；两个 8/8 全成功 batch 中 anchor 仍为 `-2/7`，
> perturbed 为 `+2/7`。这与独立评测的 anchor regression 方向一致。下一步先实现
> mode-stratified RLOO 的 CPU helper/tests，使两个模式各自 leave-one-out；不直接
> 延长训练或立即加入更复杂 KL。详见
> `docs/RA_LOOP_REWARD_INTERFERENCE_DIAGNOSIS_20260721.md`。

> **mode-stratified RLOO CPU helper 已完成：2026-07-21** — 新增严格按
> `(rollout_group_id, is_perturbed)` 分层的 NumPy leave-one-out helper；全成功归零、
> 模式隔离、多 group、padding 与 fail-closed 输入均已覆盖。目标文件 24/24、全项目
> 38/38 tests 通过。历史 35-step reward replay 中，101 条成功 anchor 的负
> advantage 从 39 条降为 0（57 条为正、44 条为零），两个模式的组内 advantage
> 和均为零。当前只完成数学核心，尚未接入 optimizer、未使用 simulator/GPU。下一步
> 做 exactly-once 且不复制上游 PPO 语义的 optimizer 接入设计与 CPU 测试。

> **mode-stratified optimizer 接入完成：2026-07-21** — 目标 advantage 通过
> `(K-1)/K` surrogate reward 可逆编码，使未修改的上游 K-way RLOO 精确还原分层
> advantage；PPO、clipping、backward 与保存路径不复制。真实父优化器捕获测试在
> `1e-15` 精度内匹配 helper，rollout/reward hooks 在成功和异常路径均恢复。Hydra
> 显式配置 `advantage_mode=mode_stratified`，同时保留 `upstream` 供旧目标 A/B。
> 全项目 40/40 tests、四任务 CPU-only factory preflight passed，`create_env=false`，
> 未使用 GPU。下一步仅做一任务一步 GPU connectivity smoke，不直接长训。

> **mode-stratified GPU connectivity smoke 已启动：2026-07-21 22:09 CST** — 经
> CPU preflight 后在 GPU 7、shared tmux `ra_loop_stratified_connectivity_smoke`
> 启动一任务一步 K8/h220 smoke。使用 pilot step5 warm-start、fixed-L2 0.1、
> lambda 0.5、LR1e-5，独立输出且不保存 checkpoint。启动前 GPU 为 18 MiB、0%、
> 41°C；模型/LoRA/headers 加载后约 18437 MiB、49°C，已进入第 2/8 rollout。
> 预计 12--15 分钟；该运行只验接线，不作为性能证据。详见
> `docs/RA_LOOP_STRATIFIED_SMOKE_20260721.md`。

> **mode-stratified GPU connectivity smoke 已完成：2026-07-21 22:21 CST** —
> 8/8 rollout、1/1 PPO update、`Finished training`、exit 0。anchor 与 perturbed
> 均为 2/4 success；`advantage_mode_stratified=1`，两组 mean advantage 分别约
> `5.55e-17` 与 0，同时 `non_zero_adv_ratio=1.0`、model/header gradient 均非零，
> 证明分层均值归零但个体学习信号存在。单步循环 11分02秒，无 traceback/OOM，按
> 设计未保存 checkpoint；GPU 7 已释放至 18 MiB、0%、41°C。端到端接线 gate 通过，
> 下一步准备小规模同 warm-start/seed 的 upstream、lambda=0、stratified A/B，不直接
> 长训。详见 `docs/RA_LOOP_STRATIFIED_SMOKE_20260721.md`。

> **mode-stratified 四任务长训已启动：2026-07-21 22:32 CST** — 冻结提交
> `1275e15` 已推送；40/40 tests 与四任务 35-step CPU factory preflight passed。
> 在 GPU 7、tmux `ra_loop_stratified_multitask:run1` 启动与旧下午 run 的单变量
> 对照：同 pilot step5 warm-start、四任务、35 updates、K8/h220、fixed-L2 0.1、
> LR1e-5，只将 advantage 改为 mode-stratified。模型/LoRA/headers 和四环境均加载
> 成功，已进入 step 1 的第 3/8 rollout；GPU 约 17861 MiB、47°C。预计 6--8 小时，
> 完成后评测 checkpoint，不按在线成功率选型。详见
> `docs/RA_LOOP_STRATIFIED_MULTITASK_READY_20260721.md`。

> **mode-stratified 四任务长训已完成：2026-07-22 04:28 CST** — 35/35
> updates、训练循环 5小时55分03秒、exit 0，GPU 7 已释放。在线 280 rollouts 为
> 193 success（anchor 95/140、perturbed 98/140）；35/35 steps 确认 stratified
> 模式，28 steps 有非零 advantage，七个组内 reward 一致的 step 正确零更新。
> mean mode advantages 保持零和，PPO ratio/clipfrac/gradient 无数值发散。step
> 5/10/15/20/25/30 六份 checkpoint 均通过 879 LoRA tensors、headers、rank32
> 的 CPU 读取，无 `.incomplete`。在线数字因 task/init 采样不同不能与旧 run 直接
> 作性能结论；下一步必须运行完全匹配的 288-episode paired evaluation。详见
> `docs/RA_LOOP_STRATIFIED_MULTITASK_RESULT_20260722.md`。

> **stratified 六 checkpoint 独立评测已启动：2026-07-22 09:13 CST** — 冻结
> 提交 `14b77a1` 已推送；24/24 CPU plans、40/40 tests passed。step
> 5/10/15/20/25/30 分别在 GPU 1--6 并行，每份四任务 ×（6 anchor + 6
> fixed-L2 0.1），总计 288 episodes，init/seed 与 baseline 和旧 objective 精确
> 匹配。六份 LoRA/headers 均加载并进入首个 episode；GPU 各约 16655 MiB、
> 46--48°C。预计 45--60 分钟，完成前不使用中间结果。详见
> `docs/RA_LOOP_STRATIFIED_CHECKPOINT_EVAL_20260722.md`。

> **stratified 六 checkpoint 独立评测已完成：2026-07-22 10:00 CST** — 六份
> exit 0、24 summaries、288/288 rows、无 `.incomplete`，GPU 1--6 已释放。
> baseline 为 24/24 anchor、21/24 perturbed、45/48；stratified step 10/20
> 达到 24/24、21/24、45/48，优于旧 objective 最佳 44/48，但未超过 baseline。
> 新目标恢复了旧训练的 anchor regression，并修复 stove `fixed_l2/init 2`，却稳定
> 损失 original `fixed_l2/init 1`，形成一换一；drawer 两个失败仍未修复。step30
> 回落到 44/48，不支持原配置继续加步数。下一步做 stratified lambda recovery
> 0/0.25 的长时权重消融。详见
> `docs/RA_LOOP_STRATIFIED_CHECKPOINT_EVAL_RESULT_20260722.md`。

> **stratified lambda 权重消融已启动：2026-07-22 12:46 CST** — 两组 35-step
> 长训在共享 tmux `ra_loop_stratified_lambda_sweep` 并行运行：lambda=0 使用 GPU 7，
> lambda=0.25 使用 GPU 6；GPU 0 未使用。两边均已加载同一 step-5 warm-start、
> OpenVLA/LoRA/headers 和四个环境，并进入 step 1/35 首个 rollout。启动后显存约为
> GPU 7 18.3 GiB、GPU 6 18.0 GiB，温度 43/45°C；预计各约 6 小时。

> **stratified lambda 权重消融训练已完成：2026-07-22 18:47 CST** — lambda=0
> 与 0.25 两组均完成 35/35、`Finished training`、exit 0，训练循环分别为
> 5:57:44 和 6:00:48，GPU 6/7 已释放。在线成功数分别为 196/280
> （anchor 100、perturbed 96）和 187/280（98、89）；由于 dynamic skip 导致两组
> 任务采样次数不同，该数字仅作健康检查，不能直接选择权重。两组各六份 checkpoint
> 均完成张量级验收：LoRA 879 tensors、headers 34 tensors、无 incomplete。下一步是
> 对十二份 checkpoint 做相同 paired 独立评测。详见
> `docs/RA_LOOP_STRATIFIED_LAMBDA_SWEEP_RESULT_20260722.md`。

> **lambda 十二 checkpoint 独立评测已启动：2026-07-22 20:49 CST** — 使用
> GPU 2--7 六卡两轮调度，总计 12 checkpoints、576 paired episodes；每卡串行一个
> lambda=0 和一个 lambda=0.25 checkpoint，顺序交叉。tmux 为
> `ra_loop_stratified_lambda_checkpoint_eval`。六个首轮 worker 均已加载 LoRA/header
> 并进入 episode，显存约 16.7 GiB、温度 45--46°C，预计总耗时 90--120 分钟。

> **lambda 十二 checkpoint 独立评测已完成：2026-07-22 22:21 CST** — 12/12
> exit 0、48 summaries、576/576 rows、无 incomplete，GPU 2--7 已释放。
> lambda=0 的 step 5/30 为 24/24 anchor、21/24 fixed-L2、45/48，与 baseline
> 逐条一致；其他 lambda=0 checkpoints 为 44/48。lambda=0.25 最佳仅 44/48，最低
> 43/48。既有 lambda=0.5 最佳也是 45/48，但只是一换一修复。恢复权重消融没有任何
> checkpoint 超过 baseline，因此停止扫 lambda，不替换 warm-start；下一步验证
> 10-task、更多 demos/init 的覆盖不足假设。详见
> `docs/RA_LOOP_STRATIFIED_LAMBDA_CHECKPOINT_EVAL_RESULT_20260722.md`。

> **Spatial 全任务 lambda 2×2 长训已启动：2026-07-22 23:39 CST** — 四个独立
> 单卡 run 在 tmux `ra_loop_fulltask_lambda_ablation` 并行运行：lambda=0.5 的
> seed10000/20000 使用 GPU 7/6，lambda=0 的匹配 seed 使用 GPU 5/4。每组为
> 10/10 tasks、50 demos/task、100 updates、K8/h220、fixed-L2 0.1、mode-stratified、
> 同一 pilot step-5 warm-start。四组均已实际加载 500-demo dataset、创建 10 个环境并
> 进入 step 1/100 首个 rollout。启动前磁盘可用 212 GB，预计新增 45--48 GB，墙钟
> 17--22 小时。详见 `docs/RA_LOOP_FULLTASK_LAMBDA_ABLATION_READY_20260722.md`。

> **Spatial 全任务 lambda 2×2 长训已完成：2026-07-23 16:43 CST** — 四组均为
> 100/100、`Finished training`、exit 0，耗时 16:30--17:03，GPU 4--7 已释放。
> lambda=0.5 两 seed 在线为 557/800、572/800；lambda=0 为 585/800、593/800；
> 在线数字受训练路径影响，仅作健康检查。四组各保存 step10--90 九份 checkpoint，
> 共 36 份，全部通过 LoRA 879/header 34 tensors 读取，无 incomplete，总计约 38 GB，
> 当前磁盘可用 175 GB。下一步先评 warm-start baseline 与每 run 的 step30/60/90，
> 再决定是否补邻近 checkpoint。详见
> `docs/RA_LOOP_FULLTASK_LAMBDA_ABLATION_RESULT_20260723.md`。

## 当前阶段

正在执行 Phase 1 / Baseline 复现。联合 checkpoint 的 LIBERO-Plus
Robot-init 完整评测已经结束；当前开始准备官方 31.9% OpenVLA-OFT 所需的
suite-specific checkpoints。尚未开始训练，也尚未启动新一轮 GPU 评测。

## 已完成

1. OpenVLA-OFT_m（四套件 mix-SFT / union checkpoint）完成 Robot-init 全量
   1550 条评测：`393/1550 = 25.354839%`。
2. 1550/1550 覆盖完整，missing=0、duplicate=0；checkpoint、suite、seed、
   manifest SHA 和 OSMesa 元数据检查通过。
3. 已确认正确官方对照是 OFT_m Robot `21.7%`，不是分套件 OpenVLA-OFT 的
   `31.9%`。详细记录见 `docs/BASELINE_OFTM_ROBOT1550_2026-07-20.md`。
4. 本地已有 `libero_10` suite-specific checkpoint：
   `/home/imc/models/ra-loop/openvla-oft-long`（约 16 GB）。

## 正在进行

此处保留第一个缺失模型下载期间的历史快照；该下载随后已完成并验证通过：

```text
Repository: moojink/openvla-7b-oft-finetuned-libero-spatial
Target:     /home/imc/models/ra-loop/openvla-oft-spatial
Tmux:       ra_download_spatial
Started:    2026-07-20 00:11:37 CST
```

截至快照时间：

```text
Downloader process: running (python3, PID 811681)
Local size:         515 MB
Incomplete files:   6
Completed examples: metadata/tokenizer files and proprio projector
Large files:        model shards, LoRA adapter and action head downloading
```

这属于正常的下载中间状态，`.incomplete` 文件不能用于加载或评测。可在另一终端
查看实时进度：

```bash
tmux attach -t ra_download_spatial
```

退出观察但保持下载：`Ctrl-b`，再按 `d`。不要在下载窗口按 `Ctrl-C`。

## 资源与安全状态

- 下载前磁盘剩余约 206 GB；union 与 libero_10 checkpoint 合计约 31 GB。
- 当前只进行网络与磁盘写入，没有启动 evaluator、训练或 checkpoint 修改。
- 下载不会使用 GPU；GPU 评测必须在 checkpoint 完整性校验后另行确认启动。
- 不删除 union checkpoint 或任何已有结果；下载中断时保留 Hugging Face 缓存，
  后续使用相同命令安全续传。

## 下一步（按顺序逐步执行）

1. ~~等待 spatial checkpoint 下载完成。~~ 已完成。
2. ~~执行 CPU-only 静态校验并记录 SHA-256。~~ 已完成，见
   `docs/CHECKPOINT_SPATIAL_20260720.md`。
3. ~~准备 suite-specific spatial evaluator/launcher，并完成 CPU-only dry-run。~~
   已完成；启动器为 `eval/launch_oft_spatial_robot350_tmux.sh`。
4. ~~启动并完成 spatial Robot-init 350 条 GPU 评测。~~ 已完成，结果为
   135/350（38.5714%）。
5. ~~完成 object checkpoint、dry-run 和 398 条正式评测。~~ 已完成，结果为
   137/398（34.4221%）。下一步处理 goal，之后使用现有 long checkpoint 重跑
   `libero_10`。四个 suite 合并后才与官方 OpenVLA-OFT Robot 31.9% 对比。
